import { View, Text } from "react-native";
import { StatusBar } from "expo-status-bar";

export default function Devices() {
  return (
    <View className="flex-1 bg-white py-10 px-6">
      <Text className="text-4xl font-bold py-4">Devices</Text>
    </View>
  );
}
